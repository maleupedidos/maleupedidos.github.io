
# Fotos del CRM: NO se publican. Tienen nombre, telefono, lote y deuda de
# clientes reales, y GitHub Pages sirve este repo publico. (19/08/2026)
crm-clientes-snapshot.json
crm-lotes-snapshot.json

# Dependencias del build (se instalan con: npm install)
node_modules/

# Capturas de las pruebas con navegador (Playwright). No se publican.
/*.png

# Capturas y snapshots que deja el navegador al verificar. No van al repo.
.playwright-mcp/
